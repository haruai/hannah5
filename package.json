{
  "name": "a-little-bouquet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "playwright test",
    "art:prepare": "node scripts/prepare-art.mjs && node scripts/prepare-decor.mjs && node scripts/prepare-idle.mjs && node scripts/prepare-memory.mjs && node scripts/prepare-plant.mjs && node scripts/prepare-time.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "sharp": "^0.35.4",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@fontsource/vt323": "^5.3.0"
  }
}
